import Vue from "vue";
import Vuex from "vuex";

import mapControls from "@/store/mapControls";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  getters: {},
  modules: {
    mapControls
  }
});
